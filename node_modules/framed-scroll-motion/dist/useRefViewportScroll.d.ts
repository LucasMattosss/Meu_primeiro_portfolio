import { RefObject } from 'react';
export declare type InputRange = number[];
/**
 *
 * @param keyframes Viewport keyframes must be ascending decimal values representing the refs viewport scroll percentage
 * @returns [ref, inputRange]
 */
export declare function useRefViewportScroll(keyframes?: number[]): [ref: RefObject<HTMLDivElement>, inputRange: InputRange];

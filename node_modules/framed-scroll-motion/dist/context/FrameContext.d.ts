import { ReactNode } from 'react';
declare type ContextProps = {
    width: number | null;
    height: number | null;
};
export declare const FrameProvider: ({ children, }: {
    children: ReactNode | ReactNode[];
}) => JSX.Element;
export declare function useWindowSize(): ContextProps;
export {};
